# Standings Site

## Dependencies:

- `wheel`
- `Flask`
- `uWSGI`

## Setup

- Your local reverse proxy is probably better at serving the files in `static/`; they should be served from the server root.
- Set up a service that kicks this online on startup

